export function TensAndUnits({value}:{value:number}){const d=Math.floor(value/10),u=value%10;return <div className="base-ten"><div><label>DECENAS</label><div className="tens">{Array.from({length:d},(_,i)=><span className="ten-bar" key={i}>{Array.from({length:10},(_,j)=><i key={j}/>)}</span>)}</div></div><b>+</b><div><label>UNIDADES</label><div className="units">{Array.from({length:u},(_,i)=><i key={i}/>)}</div></div></div>}
